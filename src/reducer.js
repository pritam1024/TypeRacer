initState = {
  randText: "Loading ...",
  splitA: "",
  splitB: "Loading ...",
  text: "",
  textlen: 0,
  index: 0
};

function reducer(state = initState, action) {
  switch (action.type) {
    case "MOD":
      if (action.element == "randText") {
      }
  }
}
